name: Benchmark Check

on:
  push:
  pull_request:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate benchmark and metrics assets
        run: ./scripts/check-improvement-systems.sh
