name: Metrics Validation

on:
  push:
  pull_request:

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate workflow metrics and regression assets
        run: ./scripts/check-improvement-systems.sh
