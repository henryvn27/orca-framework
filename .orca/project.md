# ORCA Project

Canonical state: Notion when configured; markdown fallback otherwise.

Configure Notion by setting project/page/data-source IDs in `.orca/config.env`. Optional sync command support lives in `notion_sync_command` or `ORCA_NOTION_SYNC_COMMAND`.
Linear stays optional and explicit.
